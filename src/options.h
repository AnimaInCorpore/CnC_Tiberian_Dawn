#ifndef OPTIONS_H
#define OPTIONS_H

/*
**  Minimal placeholder for the legacy OptionsClass.
**
**  The original `options.h` is not ported yet; some modules (e.g. `goptions.*`)
**  only need the base type to exist so they can compile.
*/

class OptionsClass {
public:
    OptionsClass(void) {}
    virtual ~OptionsClass() {}
};

#endif

