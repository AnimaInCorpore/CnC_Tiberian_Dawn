#include "function.h"
#include "gamedlg.h"

void GameControlsClass::Process(void)
{
    /*
    ** Stub: the full dialog depends on the unported `OptionsClass` and
    ** the UI control stack (`GaugeClass`/`SliderClass`).
    */
}

