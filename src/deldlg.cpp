#include "function.h"

/*
 * Legacy makefiles reference a deldlg.cpp unit, but the original source is not
 * present in this repository snapshot. Keep a stub translation unit so build
 * inventories remain stable while porting.
 */
