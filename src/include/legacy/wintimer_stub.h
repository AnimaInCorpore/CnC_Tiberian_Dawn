#ifndef WINTIMER_STUB_H
#define WINTIMER_STUB_H

// Dummy WinTimerClass for porting purposes
class WinTimerClass {
    // Add any necessary members or methods if they are accessed
public:
    // Destructor might be called, so define an empty one
    ~WinTimerClass() {}
};

#endif // WINTIMER_STUB_H