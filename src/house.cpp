// House module port placeholder.
// The original HOUSE.CPP is large and depends on many legacy headers.
// For incremental porting we currently use the lightweight shim in
// `legacy_compat.h`. When ready to compile the original implementation
// move its contents into src/ and remove the shim.

